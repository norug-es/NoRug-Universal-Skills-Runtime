import {userFromRequest} from '@/lib/user-auth';import {verifyCryptoIntent} from '@/lib/stablecoin';export async function POST(req:Request){const u=await userFromRequest(req);if(!u)return Response.json({error:'unauthorized'},{status:401});const b=await req.json().catch(()=>({}));if(!b.intent_id||!b.tx_hash)return Response.json({error:'intent_id_and_tx_hash_required'},{status:400});try{return Response.json(await verifyCryptoIntent(u.id,String(b.intent_id),String(b.tx_hash)))}catch(e:any){const m=String(e?.message||e);return Response.json({error:m},{status:/not_confirmed/.test(m)?409:400})}}
