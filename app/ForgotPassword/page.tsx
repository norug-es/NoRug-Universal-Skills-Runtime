import {ForgotForm} from '@/components/AuthForms';export default function Page(){return <main className="dashShell"><a className="back" href="/Login">← Login</a><div className="dashHead"><div className="eyebrow">Account recovery</div><h1>Recover access</h1><p>We will email a short-lived reset link if the account exists.</p></div><div className="loginCard"><ForgotForm/></div></main>}
