import {DocsShell} from '@/components/DocsShell';export default function Page(){return <DocsShell lang="es" title="Documentación"><h1>NoRug Universal Skills Runtime</h1><p>Capa universal que conecta skills portables y todas las capacidades declaradas por SWAT Core con ChatGPT/OpenAI, Claude, OpenClaw, Manus, Ollama y clientes REST/MCP.</p><h2 id="install">Instalación en Dokploy</h2><p>Despliega el repositorio como servicio Docker Compose. Configura <code>skills.norug.es</code> desde Domains apuntando al servicio <code>skills</code>, puerto 3000. El compose usa <code>expose</code>, no publica el puerto del host y no fija <code>container_name</code>.</p><pre className="code">SWAT_CORE_BASE_URL=https://bitcoiners.norug.es{`\n`}SWAT_CORE_OPENAPI_URL=https://bitcoiners.norug.es/openapi.json{`\n`}SWAT_CORE_API_KEY=&lt;secret&gt;{`\n`}SKILLS_GATEWAY_API_KEY=&lt;secret&gt;{`\n`}SWAT_EXECUTION_ENABLED=false{`\n`}SWAT_ALLOW_WRITES=false{`\n`}SWAT_ALLOW_ADMIN=false</pre><h2 id="architecture">Arquitectura</h2><pre className="code">AI clients → MCP / REST / tool calling → skills.norug.es{`\n`}→ OpenAPI capability importer → policy gates → SWAT Core{`\n`}→ evidence / QA / provenance</pre><p>El registry SWAT no duplica endpoints: se genera dinámicamente desde el OpenAPI canónico. Todos los métodos y tags entran en discovery; la ejecución se controla aparte.</p><h2 id="api">REST API</h2><ul><li><code>GET /api/v1/health</code></li><li><code>GET /api/v1/skills</code> — 25 skills financieras portables.</li><li><code>GET /api/v1/core/capabilities</code> — 100% de operaciones del Core.</li><li><code>POST /api/v1/core/describe</code></li><li><code>POST /api/v1/core/execute</code> — sujeto a API key y policy gates.</li><li><code>GET /api/v1/adapters/openai/tools</code></li><li><code>GET /api/v1/adapters/ollama/tools</code></li><li><code>GET /api/v1/openapi.json</code></li></ul><h2 id="mcp">MCP</h2><p>Endpoint remoto: <code>https://skills.norug.es/mcp</code>. Publica las herramientas internas de skills y convierte cada operación actual del OpenAPI de SWAT en una tool MCP.</p><h2 id="providers">Plataformas AI</h2><p><strong>ChatGPT/OpenAI:</strong> MCP remoto o tools REST. <strong>Claude:</strong> MCP remoto. <strong>OpenClaw:</strong> MCP streamable HTTP. <strong>Manus:</strong> MCP connector. <strong>Ollama:</strong> tool calling usando el adapter REST.</p><h2 id="security">Seguridad</h2><p>La API key del Core vive solo en el servidor. Discovery no implica permiso de ejecución. Lecturas, escrituras y administración tienen gates separados. La configuración inicial recomendada bloquea toda ejecución hasta validar el E2E.</p><h2 id="testing">Pruebas</h2><p>Cada proveedor debe demostrar conexión, descubrimiento completo, lectura pública, lectura protegida, denegación de write/admin con gates cerrados y equivalencia de capability IDs. Guardar proveedor, modelo, versión runtime, hash OpenAPI, capability, estado y hash de respuesta.</p><h2 id="sponsors">Sponsors y referencias</h2><p>Cada sponsor puede tener un código principal y múltiples campañas. Los enlaces <code>/r/&lt;codigo&gt;</code> crean atribución first-touch. También se aceptan <code>x-norug-referral-code</code> y <code>x-sponsor-code</code> para clientes API/MCP. El equipo interno puede ver referidos pseudonimizados y consumo por servicio; cada sponsor accede a <code>/Sponsor</code> con un token propio y solo ve métricas agregadas de su canal.</p><p>Administración: <code>/Dashboard/Sponsors</code>. API: <code>GET/POST /api/v1/admin/sponsors</code>, <code>POST /api/v1/admin/sponsors/&lt;id&gt;/campaigns</code> y <code>GET /api/v1/sponsor/summary</code>.</p><h2 id="analytics">Analítica y trazabilidad</h2><p>El gateway registra automáticamente llamadas, usuarios/principals pseudonimizados, sesiones, proveedor/modelo AI, skill/capability, fecha/hora, latencia, errores y entidades blockchain detectadas. <code>/Stats</code> muestra métricas agregadas; <code>/Dashboard</code> es privado y muestra la telemetría operativa detallada. El texto íntegro de consultas no se guarda por defecto.</p><h2 id="credits">Créditos y continuidad</h2><p>La cuota mensual se consume primero y los créditos comprados después. El runtime registra hitos al 50%, 80%, 90% y 100%. Si no queda saldo, la ejecución devuelve 402 y muestra la fecha de renovación, compra de créditos o cambio de plan. Los top-ups se pagan con Stripe o con las rutas USDC/USDT configuradas.</p></DocsShell>}
