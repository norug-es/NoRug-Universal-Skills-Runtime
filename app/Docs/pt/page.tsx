import {DocsShell} from '@/components/DocsShell';export default function Page(){return <DocsShell lang="pt" title="Documentação"><h1>NoRug Universal Skills Runtime</h1><p>Camada universal que conecta skills portáteis e todas as operações OpenAPI do SWAT Core ao ChatGPT/OpenAI, Claude, OpenClaw, Manus, Ollama e clientes REST/MCP.</p><h2 id="install">Instalação no Dokploy</h2><p>Implante como serviço Docker Compose. Configure <code>skills.norug.es</code> em Domains para o serviço <code>skills</code>, porta 3000. O compose usa <code>expose</code>, não publica porta no host e não define <code>container_name</code>.</p><pre className="code">SWAT_CORE_BASE_URL=https://bitcoiners.norug.es{`\n`}SWAT_CORE_OPENAPI_URL=https://bitcoiners.norug.es/openapi.json{`\n`}SWAT_CORE_API_KEY=&lt;secret&gt;{`\n`}SKILLS_GATEWAY_API_KEY=&lt;secret&gt;{`\n`}SWAT_EXECUTION_ENABLED=false{`\n`}SWAT_ALLOW_WRITES=false{`\n`}SWAT_ALLOW_ADMIN=false</pre><h2 id="architecture">Arquitetura</h2><pre className="code">Clientes AI → MCP / REST / tool calling → skills.norug.es{`\n`}→ importador OpenAPI → policy gates → SWAT Core{`\n`}→ evidence / QA / provenance</pre><p>O registry SWAT é gerado dinamicamente pelo contrato OpenAPI canônico. Todas as operações entram em discovery; permissões de execução são independentes.</p><h2 id="api">REST API</h2><ul><li><code>GET /api/v1/health</code></li><li><code>GET /api/v1/skills</code> — 25 skills financeiras portáteis.</li><li><code>GET /api/v1/core/capabilities</code> — todas as operações do Core.</li><li><code>POST /api/v1/core/describe</code></li><li><code>POST /api/v1/core/execute</code> — protegido por API key e policies.</li><li><code>GET /api/v1/adapters/openai/tools</code></li><li><code>GET /api/v1/adapters/ollama/tools</code></li><li><code>GET /api/v1/openapi.json</code></li></ul><h2 id="mcp">MCP</h2><p>Endpoint remoto: <code>https://skills.norug.es/mcp</code>. Publica ferramentas internas e converte cada operação atual do OpenAPI do SWAT Core em uma tool MCP.</p><h2 id="providers">Plataformas AI</h2><p><strong>ChatGPT/OpenAI:</strong> MCP remoto ou REST tools. <strong>Claude:</strong> MCP remoto. <strong>OpenClaw:</strong> MCP streamable HTTP. <strong>Manus:</strong> MCP connector. <strong>Ollama:</strong> tool calling via adapter REST.</p><h2 id="security">Segurança</h2><p>A chave do Core fica apenas no servidor. Discovery não concede execução. Leituras, escritas e administração possuem gates separados. O primeiro deployment deve manter execução desativada até aprovação E2E.</p><h2 id="testing">Testes</h2><p>Cada provedor deve provar conexão, discovery completo, leitura pública, leitura protegida, bloqueio de write/admin com gates fechados e equivalência de capability IDs. Registre provedor, modelo, versão, hash OpenAPI, capability, status e hash da resposta.</p><h2 id="sponsors">Sponsors e referências</h2><p>Cada sponsor pode ter um código principal e várias campanhas. Links <code>/r/&lt;código&gt;</code> criam atribuição first-touch imutável. Clientes API/MCP também podem enviar <code>x-norug-referral-code</code> ou <code>x-sponsor-code</code>. A equipe interna vê usuários referidos pseudonimizados e consumo por serviço; cada sponsor acessa <code>/Sponsor</code> com seu próprio token e vê somente métricas agregadas do seu canal.</p><p>Administração: <code>/Dashboard/Sponsors</code>. API: <code>GET/POST /api/v1/admin/sponsors</code>, <code>POST /api/v1/admin/sponsors/&lt;id&gt;/campaigns</code> e <code>GET /api/v1/sponsor/summary</code>.</p><h2 id="analytics">Analytics e rastreabilidade</h2><p>O gateway registra automaticamente chamadas, usuários/principals pseudonimizados, sessões, provedor/modelo de IA, skill/capability, data/hora, latência, erros e entidades blockchain detectadas. <code>/Stats</code> mostra métricas agregadas; <code>/Dashboard</code> é privado e mostra telemetria operacional detalhada. O texto integral das consultas não é armazenado por padrão.</p><h2 id="credits">Créditos e continuidade</h2><p>A franquia mensal é consumida primeiro e os créditos comprados depois. O runtime registra marcos de 50%, 80%, 90% e 100%. Sem saldo disponível, a execução retorna 402 e mostra renovação, compra de créditos ou upgrade. Top-ups aceitam Stripe e rotas USDC/USDT configuradas.</p></DocsShell>}
