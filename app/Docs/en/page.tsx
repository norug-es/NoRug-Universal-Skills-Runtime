import {DocsShell} from '@/components/DocsShell';export default function Page(){return <DocsShell lang="en" title="Documentation"><h1>NoRug Universal Skills Runtime</h1><p>A universal capability layer connecting portable skills and every SWAT Core OpenAPI operation to ChatGPT/OpenAI, Claude, OpenClaw, Manus, Ollama and REST/MCP clients.</p><h2 id="install">Dokploy installation</h2><p>Deploy as a Docker Compose service. Configure <code>skills.norug.es</code> in Dokploy Domains to service <code>skills</code>, port 3000. The compose file uses <code>expose</code>, does not publish a host port and does not set <code>container_name</code>.</p><pre className="code">SWAT_CORE_BASE_URL=https://bitcoiners.norug.es{`\n`}SWAT_CORE_OPENAPI_URL=https://bitcoiners.norug.es/openapi.json{`\n`}SWAT_CORE_API_KEY=&lt;secret&gt;{`\n`}SKILLS_GATEWAY_API_KEY=&lt;secret&gt;{`\n`}SWAT_EXECUTION_ENABLED=false{`\n`}SWAT_ALLOW_WRITES=false{`\n`}SWAT_ALLOW_ADMIN=false</pre><h2 id="architecture">Architecture</h2><pre className="code">AI clients → MCP / REST / tool calling → skills.norug.es{`\n`}→ OpenAPI capability importer → policy gates → SWAT Core{`\n`}→ evidence / QA / provenance</pre><p>The SWAT registry is generated from the canonical OpenAPI contract instead of duplicating endpoints. Every HTTP operation enters discovery; execution permissions are separate.</p><h2 id="api">REST API</h2><ul><li><code>GET /api/v1/health</code></li><li><code>GET /api/v1/skills</code> — 25 portable finance skills.</li><li><code>GET /api/v1/core/capabilities</code> — all Core operations.</li><li><code>POST /api/v1/core/describe</code></li><li><code>POST /api/v1/core/execute</code> — API-key and policy gated.</li><li><code>GET /api/v1/adapters/openai/tools</code></li><li><code>GET /api/v1/adapters/ollama/tools</code></li><li><code>GET /api/v1/openapi.json</code></li></ul><h2 id="mcp">MCP</h2><p>Remote endpoint: <code>https://skills.norug.es/mcp</code>. It exposes built-in skills tools and converts every current SWAT Core OpenAPI operation into an MCP tool.</p><h2 id="providers">AI providers</h2><p><strong>ChatGPT/OpenAI:</strong> remote MCP or REST tools. <strong>Claude:</strong> remote MCP. <strong>OpenClaw:</strong> streamable HTTP MCP. <strong>Manus:</strong> MCP connector. <strong>Ollama:</strong> tool calling through the REST adapter.</p><h2 id="security">Security</h2><p>The SWAT Core key stays server-side. Discovery does not grant execution. Read, write and administration are separately gated. Initial deployment should keep execution disabled until E2E validation passes.</p><h2 id="testing">Testing</h2><p>Each provider must prove connectivity, complete discovery, public read, protected read, denied write/admin while gates are closed and equivalent capability IDs. Retain provider/model/runtime version/OpenAPI hash/capability/status/response hash.</p><h2 id="sponsors">Sponsors and referrals</h2><p>Each sponsor can have a primary code and multiple campaigns. <code>/r/&lt;code&gt;</code> links create immutable first-touch attribution. API/MCP clients may also send <code>x-norug-referral-code</code> or <code>x-sponsor-code</code>. Internal operators can inspect pseudonymous referred users and service usage; each sponsor can access <code>/Sponsor</code> with its own portal token and only see aggregate metrics for its channel.</p><p>Administration: <code>/Dashboard/Sponsors</code>. API: <code>GET/POST /api/v1/admin/sponsors</code>, <code>POST /api/v1/admin/sponsors/&lt;id&gt;/campaigns</code> and <code>GET /api/v1/sponsor/summary</code>.</p><h2 id="analytics">Analytics and traceability</h2><p>The gateway automatically records calls, pseudonymous users/principals, sessions, AI provider/model, skill/capability, timestamps, latency, errors, and detected blockchain entities. <code>/Stats</code> exposes aggregate metrics; <code>/Dashboard</code> is private and exposes detailed operational telemetry. Full query text is not retained by default.</p><h2 id="credits">Credits and continuity</h2><p>Monthly allocation is consumed first and purchased credits afterwards. The runtime records 50%, 80%, 90% and 100% milestones. If no spendable balance remains, execution returns 402 and shows renewal, top-up and upgrade actions. Top-ups support Stripe and configured USDC/USDT routes.</p></DocsShell>}
