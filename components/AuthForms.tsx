'use client';
import {useEffect,useState} from 'react';
export function RegisterForm(){const [m,setM]=useState('');const [ref,setRef]=useState('');useEffect(()=>{const x=document.cookie.match(/(?:^|;\s*)nr_ref=([^;]+)/);if(x)setRef(decodeURIComponent(x[1]))},[]);async function submit(e:any){e.preventDefault();setM('Creating account…');const f=new FormData(e.currentTarget);const r=await fetch('/api/v1/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password'),display_name:f.get('display_name'),company:f.get('company'),sponsor_code:f.get('sponsor_code')})});const j=await r.json();if(r.ok)location.href='/Account';else setM(j.message||j.error)}return <form className="formGrid" onSubmit={submit}><input name="display_name" placeholder="Name"/><input name="email" type="email" required placeholder="Email"/><input name="company" placeholder="Company (optional)"/><input name="sponsor_code" value={ref} onChange={e=>setRef(e.target.value)} placeholder="Sponsor / referral code (optional)"/><input name="password" type="password" required placeholder="Password · 12+ chars, upper/lowercase + number"/><button>Create account</button><small className="muted">{m}</small></form>}
export function LoginForm(){const [m,setM]=useState('');async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/v1/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const j=await r.json();if(r.ok)location.href='/Account';else setM(j.error)}return <form className="formGrid" onSubmit={submit}><input name="email" type="email" required placeholder="Email"/><input name="password" type="password" required placeholder="Password"/><button>Sign in</button><small className="muted">{m}</small></form>}
export function ForgotForm(){const [m,setM]=useState('');async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/v1/auth/forgot-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email')})});const j=await r.json();setM(j.message||j.error)}return <form className="formGrid" onSubmit={submit}><input name="email" type="email" required placeholder="Email"/><button>Send recovery email</button><small className="muted">{m}</small></form>}
export function ResetForm(){const [m,setM]=useState('');async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const token=new URLSearchParams(location.search).get('token');const r=await fetch('/api/v1/auth/reset-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,password:f.get('password')})});const j=await r.json();if(r.ok){setM('Password changed. Redirecting…');setTimeout(()=>location.href='/Login',800)}else setM(j.error)}return <form className="formGrid" onSubmit={submit}><input name="password" type="password" required placeholder="New password"/><button>Set new password</button><small className="muted">{m}</small></form>}
