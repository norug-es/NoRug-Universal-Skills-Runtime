const base=process.env.BASE_URL||'http://127.0.0.1:3000';for(const path of ['/api/v1/health','/api/v1/skills','/api/v1/compatibility']){const r=await fetch(base+path);if(!r.ok)throw new Error(`${path} ${r.status}`);console.log(path,'PASS')}const rr=await fetch(base+'/api/v1/resolve',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({intent:'Build a DCF valuation'})});const j=await rr.json();if(!rr.ok||j.selected?.id!=='finance.dcf-model')throw new Error('resolve gate failed');console.log('/api/v1/resolve PASS');const m=await fetch(base+'/mcp',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({jsonrpc:'2.0',id:1,method:'tools/list'})});if(!m.ok)throw new Error('mcp failed');console.log('/mcp tools/list PASS');
